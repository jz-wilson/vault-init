# Never

Hard prohibitions. Distinct from soft preferences in IDENTITY.md — these are constraints an agent must never violate.

## Examples (replace these)

- Never commit secrets or credentials to any repository.
- Never push this vault to a public remote without approval.
- Never mix client data between project notes.
- Never skip validation or linting checks locally.

Replace each bullet with a rule you cannot break.
