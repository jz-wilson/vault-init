# Always

Deterministic hard rules. Distinct from soft preferences in IDENTITY.md — these are constraints an agent **must** always follow.

## Examples (replace these)

- Always strip PII before pushing to public repos.
- Always run the validator before committing changes.
- Always document breaking changes in a separate section.
- Always test locally before deploying to production.

Replace each bullet with a rule you cannot break.
